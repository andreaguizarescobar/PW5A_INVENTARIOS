
export const ReqRespUsuarioListado = {
    page,
    per_page,
    total,
    total_pages,
    data,
    support
}

export const Usuario = {
    id,
    email,
    first_name,
    last_name,
    avatar
}

export const Support = {
    url,
    text
}
